// Office shim for the page-editor's TgvV5Nav catalog block (mono a89dd0b, v5-parity).
// The REAL form lives in the consuming Village app (tgv.com src/lib/domains/support/…).
// Never reached in Office: the shimmed useSupportModal keeps supportOpen=false.
export default function ContactForm(_props: { dict?: unknown }) {
  return null;
}
